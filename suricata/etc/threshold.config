# Suricata Threshold Configuration File
# This file contains threshold configurations for Suricata rules
# 
# Format:
# threshold gen_id <gid>, sig_id <sid>, type <threshold|limit|both>, track <by_src|by_dst>, count <n>, seconds <t>
# suppress gen_id <gid>, sig_id <sid>
# suppress gen_id <gid>, sig_id <sid>, track <by_src|by_dst>, ip <ip|subnet>
#
# Example entries:
# threshold gen_id 1, sig_id 2100498, type both, track by_src, count 2, seconds 60
# suppress gen_id 1, sig_id 2100368

# Default threshold settings - currently empty
# Add your custom threshold rules here as needed